import Std::Io::PrintLine;

func Main() -> int {
    PrintLine("Hello, world!");
    PrintLine("Factorial calculation");
    let n = 5;
    var result = 1;
    for i in 1..=n {
        result *= i;
        PrintLine("{}! = {}", i, result);
    }
    return 0;
}