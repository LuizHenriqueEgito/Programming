const PI: f64 = 3.1415;

fn main() {
    let mut number = 10;
    number = 20;
    number += 1;

    // toda constante deve ser tipada
    const ONE_MINUTE: i32 = 60;
    const ONE_HOUR: i32 = ONE_MINUTE * 60;
}